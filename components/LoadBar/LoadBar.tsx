import LoadBarStyled from "./LoadBarStyled";

const LoadBar = (): JSX.Element => {
  return (
    <LoadBarStyled>
      <span>Loading</span>
      <div></div>
    </LoadBarStyled>
  );
};

export default LoadBar;
