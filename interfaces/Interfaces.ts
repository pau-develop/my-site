export interface IScore {
  name: string;
  player: number;
  score: number;
}

export interface CanvasProps {
  image: string;
}

export interface IGalleryItem {
  name: string;
  resolution: string;
  source: string;
}
